# Copy to .env.local and fill in values (DO NOT COMMIT .env.local)

# Azure Foundry Model Router Deployment
# Format: https://<resource-name>.services.ai.azure.com/api/projects/<project-name>
# Get this from Microsoft Foundry Portal → Your Project → Overview → Project connection string
VITE_ROUTER_ENDPOINT=https://your-resource.services.ai.azure.com/api/projects/your-project
VITE_ROUTER_API_KEY=your-api-key
VITE_ROUTER_DEPLOYMENT=model-router

# Standard Model Deployment (for comparison)
# Use the same Foundry project endpoint or a separate Azure OpenAI endpoint
VITE_STANDARD_ENDPOINT=https://your-resource.services.ai.azure.com/api/projects/your-project
VITE_STANDARD_API_KEY=your-api-key
VITE_STANDARD_DEPLOYMENT=gpt-4.1
